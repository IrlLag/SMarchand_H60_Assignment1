@model IEnumerable<SMH60Store.Models.ProductCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProdCat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Products)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.ProdCat)
            </td>
            <td>
                @Html.DisplayFor(model => item.Products)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProdCatId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProdCatId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProdCatId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
